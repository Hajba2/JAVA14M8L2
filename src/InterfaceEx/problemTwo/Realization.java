package InterfaceEx.problemTwo;

public class Realization implements IntB, IntC{

    public static void main(String[] args) {
        Realization realization = new Realization();
        realization.method();
    }
}
